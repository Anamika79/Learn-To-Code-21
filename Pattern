#include <stdio.h>

int main() {
    int r,i,j,k;
    scanf("%d",&r);
    for(i=r;i>=1;i--){
            for(j=i;j<r;j++){
                printf("\t");
            }
            for(k=1;k<=i;k++){
                printf("%d",i);
            }
            printf("\n");
    }    
}
