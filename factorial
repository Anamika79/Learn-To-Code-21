#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,i,fact=1;
    scanf("%d",&n);
    if(n>=0){
        for(i=1;i<=n;i++){
        fact = i*fact;
    }
    printf("%d",fact);
    }
    else{
        printf("ERROR");
    }
}
