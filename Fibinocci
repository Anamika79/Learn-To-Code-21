#include <stdio.h>

int main() {
    int lim,i,temp=0,n1=0,n2=1;
    scanf("%d",&lim);
    printf("%d %d",n1,n2);
    for(i=1;i<=lim-2;i++){
        temp = n1+n2;
        printf(" %d",temp);
        n1 = n2;
        n2 = temp;
    }
}
